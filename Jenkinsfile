pipeline {
  agent any
  
    stages {
    stage ('git') {
      steps {
       echo "git was cloning"
  stages {
    stage ('Build') {
      steps {
        echo "building the image"
      }
    }
    stage ('Deploy') {
      steps {
        echo "deploying the image"
      }
    }
  }
}
